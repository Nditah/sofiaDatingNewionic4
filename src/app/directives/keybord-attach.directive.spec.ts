import { KeybordAttachDirective } from './keybord-attach.directive';

describe('KeybordAttachDirective', () => {
  it('should create an instance', () => {
    const directive = new KeybordAttachDirective();
    expect(directive).toBeTruthy();
  });
});
